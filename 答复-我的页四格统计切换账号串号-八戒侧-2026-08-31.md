# 八戒答复：我的页四格统计 & H5 个人主页切换账号串号

> 日期：2026-08-31
> 回复方：八戒（pxid_h5 / likun 仓库）
> 对应文档：《我的页四格统计与个人主页_切换账号串号_Flutter侧问题点与H5整改清单-2026-08-31.md》（北帆）
> 版本参照：`likun` 仓库 `b8ce032`（含 8913c84 数字绑定根因修复；HEAD `64f9ec7` 仅含临时诊断日志）；后端 `server/server.js` ECS 与本地 md5 一致（0b1f8dde）
> 状态：**同意方案 A（token 维度为统计真值）**，`/users/me` 已完整可用、已返回 stats 四格字段；另定位并披露一个后端隐藏 bug（「骑友#F2」混合身份的来源），修复待拍板。

---

## 一、总答复（结论先行）

1. **统计真值统一到 token 维度（方案 A）**，Flutter 四格切 `/users/me`，路径可行，后端无需新增接口。
2. **`/users/me` 实测可用**：code=0，`stats{posts,favorites,following,followers}` 与 `feedCount/favoriteCount/followeeCount/followerCount` 同源同值、同时返回。
3. **附带发现一个后端 bug**：无画像 token 调 `/users/me` 时，头像/车型会串成「PXID 官方」空设备帖子的值（昵称显示骑友），这是「骑友#F2」怪异组合的真实来源，必修（见第三节）。
4. **关键约束**：`/users/me` 只认 H5 受限会话 token；**App 主 token 实测 401**。Flutter 调用必须走 Bridge `getToken/getAuthToken` 换发的受限 token，不能直传 App 主 token。

---

## 二、逐条回答「第 6 节 需回复信息清单」

### Q1：当前登录用户统计真值按哪个维度？

**答：token 对应 memberUserId（方案 A）。**

线上实测（2026-08-31 17:28，同一 H5 受限 token）：

| 接口 | 路径参数 | 返回身份 | 说明 |
| --- | --- | --- | --- |
| `GET /users/:deviceId` | deviceId=6a38cc10d48b643d | member=6「道爷」发布6 | 统计/资料**恒按路径 deviceId 查**，token 仅用于 isSelf/isFollowing（device 维度） |
| `GET /users/me` | — | member=16「骑友」全0 | token 维度 |

=> `/users/:deviceId` 语义 = **文档问题点 4 的 A/C**：统计与资料恒按路径 deviceId 查（token 仅影响 isSelf/isFollowing 判定）；「当前登录用户的我」必须用 `/users/me`。

### Q2：`/users/me` 是否完整可用？是否保证 code=0 / 四格一致 / B 非空？

**答：完整可用，三条逐项：**

1. **code=0** ✅ 实测通过（无 token 或无效 token 时 401，属预期鉴权行为）。
2. **stats 与四格字段一致** ✅ 实测 `feedCount=stats.posts`、`favoriteCount=stats.favorites`、`followeeCount=stats.following`、`followerCount=stats.followers`，同一套 SQL 计数，天然一致。
3. **nickname/avatar 对 B 非空** ⚠️ 部分满足：
   - B 在 H5 建过画像（发过帖 / 调过 `PUT /users/profile` 编辑资料）→ 返回真实昵称头像；
   - B 从未在 H5 发帖/编辑（当前场景）→ 返回默认值「骑友」+ 空头像，**这是预期行为**，因为 pxid-feed-server 里 B（member=16）确实是 0 画像 0 帖子；
   - **但**实测无画像时 avatar/carModel 会串成「PXID 官方」的值 —— 这是 bug（见第三节），修复后无画像返回：nickname=骑友、avatar=''、carModel=''，干净兜底。

### Q3：stats 字段是否保证存在？缺失时允许 Flutter 直接 0000 不回退？

**答：保证存在，同意不回退。**

- `/users/me` 与 `/users/:deviceId` 均已在返回体固定输出 `stats{posts,favorites,following,followers}`（对齐 Flutter l10n 字段名，固定不可改）；
- 承诺：code=0 时 `stats` 必有；Flutter 缺失时直接显示 0000，**不回退 `/users/:deviceId.stats`**，避免再次串号。

### Q4：他人主页跳转主键：deviceId 还是 memberUserId？

**答：建议统一 memberUserId。**

- 账号维度跨设备稳定；deviceId 在换机/恢复出厂/清缓存时会漂移，不适合做持久主键；
- 后端已兼容：`/users/:deviceId` 路径参数 device_id 或 member_user_id **双身份命中**（`feeds`/`user_profiles` 任一表按 `device_id=? OR member_user_id=?` 反解），传 memberUserId 也可正常返回；
- H5 侧 `UserProfileView` 的 `targetDevice` 目前传 deviceId，我们会改成 memberUserId 优先、deviceId 兜底。

---

## 三、后端隐藏 bug（本次排查附带发现，P0 必修）

**现象**：无画像 token 调 `/users/me` 返回 `nickname=骑友, avatar=unsplash占位图, carModel=F2` —— 三处来源拼出的混合身份。

**根因**（`server/server.js` `/users/me` 实现）：

```js
// L933 feed 兜底查询
SELECT nickname, avatar, car_model FROM feeds
WHERE (device_id=? OR member_user_id=?) AND length(nickname)>0
ORDER BY id DESC LIMIT 1        // d='' m='16' → device_id='' 命中所有空设备帖子！
```

1. token 的 deviceId 为空串（ToC 态常见）→ `device_id=''` 匹配全库所有空设备记录，实测命中「PXID 官方」的 9 条帖子（F2/F1/P2…）→ avatar/carModel 串成官方内容；
2. L951 `nickname: profile.nickname || (feed && feed.nickname) || '骑友'`：profile.nickname 默认值「骑友」为 truthy，**短路**，feed.nickname 永远用不上 → 昵称显示骑友；
3. L952/L953 avatar/carModel 用 `profile.x || feed.x` 又能取到 feed 值 → **昵称/头像/车型三个字段走了不同的兜底分支，拼出「骑友 + 官方头像 + F2」**。

**修复方案（拍板后执行）**：
- L933 feed 查询条件改为 `(? != '' AND device_id=?)` 或加 `AND length(device_id)>0`，空 device 不再命中任何帖子；
- L951-953 统一短路逻辑：profile 有值用 profile，否则 feed 有值用 feed，否则干净默认值（骑友/''/''）。

---

## 四、落地动作清单（谁改什么）

### 八戒后端（server/server.js）—— 已定位，待拍板
- [ ] 修 `/users/me` 空 device 兜底 bug（第三节方案，改完部署 + curl 复测）

### 八戒 H5（likun/src/views/UserProfileView.vue）
- [ ] 自己主页（`isSelf`）顶部昵称/头像优先用 Bridge `getUserInfo()` 的真实值（随账号切换自动刷新），不再依赖 `/users/:deviceId` 兜底（该兜底即串号根因之一）
- [ ] 他人主页跳转参数改 memberUserId 优先（后端已双身份兼容，前端传参即可）

### Flutter（北帆）—— 按文档 5.2 方案 A 执行
- [ ] 四格统计 `GET /users/:deviceId` → `GET /users/me`（token 用 Bridge 受限 token）
- [ ] 只认 `data.stats.posts/favorites/following/followers`，不回退 deviceId 接口
- [ ] 他人主页跳转传 memberUserId

---

## 五、验收标准（对齐 Flutter 第 8 节）

真机按 A → 退出 → 登录 B 跑一遍：

- [ ] Flutter「我的」四格：A 显示 A 真实值，B 显示 B 真实值（B 为 0 也是真实值，不再永远 A 的 6000）
- [ ] 进入 H5 `#/user/me?tab=publish`：
  - [ ] 顶部昵称/头像 = B 真实值（B 无画像时显示「骑友」+空头像属预期，**不再出现骑友+官方头像+F2 混合**）
  - [ ] H5 四格 = Flutter 四格，完全一致
  - [ ] 发布列表数 = 四格发布数
- [ ] 发帖：nickname 为 B 真实昵称（B 发第一帖后画像即建立，后续不再骑友）；发布统计 +1 且两端一致
- [ ] B 在 H5 编辑一次资料后，Flutter/H5 顶部均显示 B 的新昵称头像
