# PXID 车型标准清单（对齐基准）

> 用途：发现 / 精选 / 广场车型筛选与 PRD 画布对齐的权威基准。H5 `src/data/carModels.js` 已是真实数据源，本文档用于纠正 PRD / 画布里的占位代号。
> 建立：2026-08-19。状态：双 P5 已按拍板落地（展示用 `displayLabel` 加系列短前缀），定稿若调整再同步。

## 1. 展示用车型代号（权威，双 P5）

按字母序，共 11 个展示代号（P5 双显：摩P5 + 助P5）：

```
板F1 板F2 板P1 助P2 板P3 助P4 摩P5 助P5 助P6 摩P7 摩P8
```

> 展示层用 `displayLabel`（系列短前缀：电摩→摩、电助力→助、电动滑板车→板）；数据层 `code` 仍为 P5/F1…（双 P5 靠 `id` 区分）。
> 已落地：H5 `carModels.js` 的 `displayLabel` + `mock.js` 的 `plazaShowcase.name`；发现 chips / 发布选择 / 广场宫格自动生效。
> PRD 画布 §4.2 的 H10/M2/Z3、P1–P6 为占位 / 旧代号，须替换为上述真实代号。

## 2. 系列归属（真实在售）

| 系列 | 展示代号 |
| --- | --- |
| 电动滑板车 | 板F1 板F2 板P1 板P3 |
| 电助力 | 助P2 助P4 助P5 助P6 |
| 电摩 | 摩P5 摩P7 摩P8 |

注：P5 在底层对应两款在售车（电摩 P5、电助力 P5），展示代号已用系列短前缀区分（摩P5 / 助P5）。

## 3. 待定稿事项

- **P5 最终归属**：已按用户拍板双面显示（摩P5 / 助P5）。若产品定稿决定合并为单一车型或改正式命名，再同步更新 `carModels.js` 与本文档；定稿前展示稳定。
- PRD 画布车型筛选 chips 与广场宫格须按 §1 真实代号更新（删除 H10/M2/Z3，P1–P6 补全 F1/F2/P7/P8）。

## 4. 与代码的关系

- H5 `src/data/carModels.js` = 真实数据源（11 款，含双 P5 用 `id` 区分：`motorcycle-P5` / `ebike-P5`）。
- 接后端车型接口（PRD §4.4，TP-11 购车车型只读）后，整文件替换为 API 数据即可（导出名不变）。
- 本文档仅作 PRD / 画布对齐基准，不进代码。
