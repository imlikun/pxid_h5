# PXID ToC App · Flutter 需求交付清单

> 读者：**Flutter 原生开发同学**
> 说明：迁移/部署/文档由**我们（App 团队）**主导，你**只需按下方清单交付 4 样东西**，逐项回复即可，不需要你写文档或主导任何事。

---

## 0. 模块分工（快速了解你在哪）

| 模块 | 归属 | 你需要做的 |
| --- | --- | --- |
| 发现（推荐/广场/动态） | H5（我们） | 无（H5 渲染，bridge 通信） |
| 精选（商城） | H5 + Shopify | `openCheckout` 结账桥（#4） |
| 服务 | 待定 | 做原生版供对比（#2） |

---

## 1. 交付清单（4 项，按优先级）

### #1 WebView 加载方式确认（本周内，阻塞迁移排期）
- 告知：App 的 WebView 当前加载 H5 是**本地打包资源**还是**线上 URL**？
- 若是本地打包，改一行指向线上：`https://h5.pxid.com/`（最终域名以坤哥确认为准）
- 验收：浏览器打开该 URL 能看到发现/精选/服务三个模块首页

### #2 服务模块原生版效果（供对比，不阻塞）
- 按下方 §2 清单实现服务模块原生页面
- 交付：截图/录屏给坤哥，拍板最终用 H5 版还是原生版

### #3 JS Bridge 方法清单确认（本周内）
- 对照 `INTEGRATION.md`，逐项回复"已实现/未实现"：
  `getToken` / `getLocale` / `navigateTo` / `openNative` / `openShopify` / `openCheckout` / `callPhone` / `openMap`

### #4 商城结账 openCheckout 状态（与联调并行）
- 是否已按 `PXID_Shopify_结账桥接_Flutter版.md` §1 实现：
  `cartCreate → checkoutUrl → WebView 打开 → return_to 回弹`
- 回复："已完成 / 进行中 / 未开始"

---

## 2. 服务模块原生版实现范围（#2 参考）

6 入口 + 子页，视觉对齐 `tokens.css` 规范：

- 道路救援（地图/时间/电话/故障描述 + 提交）
- 使用指南（车型选择 → 新手视频 / 产品说明书）
- 车辆体检（绑定车辆 + 系统体检项 + 开始体检）
- 意见反馈（在线客服，热门问题/售后查询/使用指导 三分类 + 输入框）
- 三包政策（保修说明页）
- 附近门店（列表：距离/评分排序 + 搜索）
- 我的工单（状态 tab + 工单卡 + 详情/取消）
- 常见问题（搜索 + 筛选 + Q/A 详情）

数据接口见 `PXID_ToC_后端接口规范.md` §7。

---

## 3. 明确的边界

- **我们负责**：H5 三个模块、后端、迁移部署、域名证书、热更新、Shopify 契约。
- **你负责**：上表 4 项交付物；其余不参与。
- **你不需要做**：不写文档、不搭服务器、不碰 Shopify 店铺配置。
