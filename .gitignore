node_modules/
server/node_modules/
dist.old.*/
dist.rebuild.del.*/
.DS_Store
*.log
*.bak.*
*.bak
shots/
*.zip
*.db
*.db-shm
*.db-wal
# 凭证/密钥：本地 .env 与联调 .env.local 绝不入库；仅 .env.example 模板可入库
.env
.env.local
.env.*
!.env.example
!.gitignore

# 本地回退备份目录（含旧 discover 构建），不入库
.legacy-20260822/
# WorkBuddy 运行态数据（自动化 memory 等），属本地状态非源码，不入库
.workbuddy/
